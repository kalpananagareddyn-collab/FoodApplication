import { PriceCategoryPipe } from './price-category-pipe';

describe('PriceCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
